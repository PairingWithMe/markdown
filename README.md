# Pairing With Me - React Markdown 

React Markdown Component

```js
import Markdown from "@pairingwithme/markdown";

export default function MyComponent(props) {
  return (
    <Markdown content={`
# Title

## Subtitle

This is the example
    `} />
  );
}
```
